#include "functions.h"

using namespace std;

// Alternate way of adding words to the priority queue, char by char. Ignore this.
void addWord(string wordToEncode)
{
	for (int i = 0; i < wordToEncode.size(); i++)
	{
		//create_huff(wordToEncode[i]);
	}

}